// Toolbar items and their costs (Gold)
options = ["Belt", "Mine", "Smelter", "Fabricator"];
costs = [5, 50, 100, 250];
selected_index = 0; 

// UI Layout calculations
slot_size = 100;
spacing = 10;
total_width = (array_length(options) * slot_size) + (array_length(options) * spacing);